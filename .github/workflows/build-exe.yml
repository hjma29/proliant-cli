name: Build and Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact: proliant-cli-windows-amd64.zip
          - os: ubuntu-latest
            artifact: pcli-linux-amd64
            pyinstaller_name: pcli
          - os: macos-latest
            artifact: pcli-macos-amd64
            pyinstaller_name: pcli

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install .[all]
          pip install pyinstaller

      - name: Build binary (Windows — onedir to avoid temp DLL extraction issues)
        if: runner.os == 'Windows'
        run: |
          pyinstaller --onedir --name pcli --console src/pcli/cli.py

      - name: Build binary (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          pyinstaller --onefile --name pcli --console src/pcli/cli.py

      - name: Package Windows binary as zip
        if: runner.os == 'Windows'
        shell: pwsh
        run: Compress-Archive -Path dist\pcli\* -DestinationPath dist\proliant-cli-windows-amd64.zip

      - name: Rename binary (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: mv dist/${{ matrix.pyinstaller_name }} dist/${{ matrix.artifact }}

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/${{ matrix.artifact }}
          generate_release_notes: true

