__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
# MkDocs build output (built by .github/workflows/docs.yml, not committed)
/site/
.pytest_cache/
.coverage
htmlcov/
*.egg
hosts-ilo.ini
# SPP catalog/package download cache (not source code)
/spp/
.envrc
ilorest_logs/

.venv/

# uv lockfile — project uses setuptools/pip, uv is not part of the workflow
uv.lock

# Release build artifacts (attached to GitHub Releases, not committed)
proliant-cli-windows.zip
proliant-cli-macos
proliant-cli-linux

# Cursor editor
.cursor/
inventory.ini
# setup wizard rotating backups (contain the same credentials as inventory.ini)
inventory.ini.bak*

# Obsidian editor
.obsidian/

# Local agent workflow instructions (not shared in the repo)
AGENTS.md
